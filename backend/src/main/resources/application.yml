spring:
  datasource:
    url: jdbc:postgresql://db:5432/doodle
    password: password
    username: postgres
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  sql:
    init:
      platform: postgresql
